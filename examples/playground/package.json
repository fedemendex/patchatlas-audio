{
  "name": "patchatlas-audio-playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsdown && node scripts/copy-assets.mjs",
    "serve": "node scripts/serve.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "patchatlas-audio": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1",
    "tsdown": "^0.22.14",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
